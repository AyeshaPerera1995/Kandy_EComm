const request = require('request');
const baseUrl = require('../config/key').baseUrl;

// exports.saveNewAddress = (req, response) => {
//     const { number, street, city, state, zip } = req.body;
//     //console.log(number, street, city, state, zip)


// }